{% if auth is defined and page is defined and page.id is defined %}
    {% if auth == true and page.id|length > 0 and page.id > 0 %}
        <div><a class="btn btn-success" href="/">Bewerken</a></div>
    {% endif %}
{% endif %}